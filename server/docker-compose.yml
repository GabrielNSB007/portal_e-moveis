version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: emoveis-db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: emoveis
    ports:
      - "5433:5432"
    volumes:
      - emoveis_data:/var/lib/postgresql/data

volumes:
  emoveis_data: